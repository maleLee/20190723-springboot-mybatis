homework:
    1.把今天所有的代码全部写一遍(要求全部手写)
    2.把错误搞定！并且标注错误原因！(如果自己写的程序没有报错，则不需要较真)
    3.user role permission
        需要在xml文档中实现如下内容
        通过用户名查询出所有的用户权限信息
        自己研究thymeleaf模板，把所有查询出来的信息打印在页面上(html)，不允许用jsp(springboot官方默认是不支持jsp)
